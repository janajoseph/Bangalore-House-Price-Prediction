Client:

      -> app.html
      
      -> app.css

      -> app.js

model:

      -> columns.json(contains the Bangalore city locations names)

      -> Bangalore machine learning models

      -> machine learning model's pickle file

server:

      -> artifacts (waht we actually want to load on javaScript)

      -> server.py (contains the main server program to run)

      -> util.py (a sub-program file of server.py)  